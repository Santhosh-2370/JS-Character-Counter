# JS-Character-Counter
This project implements a character counter using HTML, CSS, and JavaScript. It allows users to type text into a textarea while displaying the number of characters entered in real time and restricting input to a fixed maximum limit.
